import dashboard from './dashboard';
import citizens from './citizens';
import users from './users';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, citizens, users]
};

export default menuItems;
